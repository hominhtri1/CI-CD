name: Manual
on: [workflow_dispatch]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      #- uses: actions/checkout@v3
      #- run: cd $GITHUB_WORKSPACE
      #- run: ls
      - run: echo ${{ secrets.TEST_SECRET }} > secrets.txt
      - run: ls secrets.txt
